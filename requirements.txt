# Backend serverless (AWS Lambda, Python 3.11) — handlers nativos.
# Sem FastAPI/uvicorn/SQLAlchemy: a migração usa handlers Lambda + psycopg2 direto.

# Banco
psycopg2-binary==2.9.9

# AWS (S3 presigned, SES) — usados por services/storage.py e services/email.py
boto3==1.34.69

# Embeddings (busca vetorial) — usado por services/embeddings.py (backend openai)
openai==1.30.1

# Utilidades
python-dotenv==1.2.2
pydantic==2.5.0
python-json-logger==2.0.7
PyJWT==2.13.0
bcrypt==4.1.1
email-validator==2.1.0

# Desenvolvimento / testes locais
pytest==9.0.3
